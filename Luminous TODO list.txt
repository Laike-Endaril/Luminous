REMEMBER TO DELETE MEMORY REPO IN MODS FOLDER!!!

See what Phosphor is doing, and specifically check if it does the optimization I did (removing data compression / decompression in World.checkLightFor())

There's a lot of unnecessary computation happening due to entities getting spawned, getting lights added, then immediately getting despawned
This would be better to solve in a different mod


Get obfuscated ASM for all 3 classes and try applying it and testing



Need to get obfuscated edited classes into the transformer somehow...
* Auto-generate asm code to create obfuscated class? (attempting)
* Write class visitors?
* See if an obfuscated bytestream works?



Improve efficiency
Fix moving light objects (don't work as well as the raw moving light test in Luminous class)
Improve mod compat (maybe ASM instead of full class replacement)



BUILD NOTE:
If any changes are made to replaced classes, build once, grab class files for them from jar, and put them in assets, then build again
