package com.fantasticsource.asmifiermod;

import net.minecraftforge.fml.common.Mod;

import static com.fantasticsource.asmifiermod.asm.ASMifierCoremod.*;

@Mod(modid = MODID, name = NAME, version = VERSION)
public class ASMifierMod
{
}
